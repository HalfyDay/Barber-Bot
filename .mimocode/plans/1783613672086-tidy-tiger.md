# План: добавление тестов для новых фич

## Контекст

Мы внедрили мульти-бизнес поддержку и zero-downtime обновления. Все 258 существующих тестов проходят. Теперь нужно покрыть тестами новые сервисы и изменения.

---

## Приоритет 1: botMenuService.js (новый сервис, 0 тестов)

**Файл**: `services/botMenuService.js`
**Тест**: `tests/botMenuService.test.js` (новый)

### Тест-кейсы:

1. **loadBotMenu — создаёт дефолтное меню если записи нет**
   - Mock: `prisma.botMenu.findUnique` возвращает `null`
   - Mock: `prisma.botMenu.create` вызывается
   - Ожидаем: возвращает дефолтное меню с screens

2. **loadBotMenu — читает существующее меню**
   - Mock: `prisma.botMenu.findUnique` возвращает `{ payload: {...} }`
   - Ожидаем: возвращает payload из БД

3. **loadBotMenu — fallback на дефолт при ошибке**
   - Mock: `prisma.botMenu.findUnique` выбрасывает ошибку
   - Ожидаем: возвращает дефолтное меню (не падает)

4. **saveBotMenu — upsert в БД**
   - Mock: `prisma.botMenu.upsert` вызывается
   - Ожидаем: возвращает меню с обновлённым `updatedAt`

5. **saveBotMenu — пробрасывает ошибку**
   - Mock: `prisma.botMenu.upsert` выбрасывает
   - Ожидаем: ошибка пробрасывается выше

---

## Приоритет 2: serverLifecycleService.js (PM2 graceful reload)

**Файл**: `services/serverLifecycleService.js`
**Тест**: `tests/serverLifecycleService.test.js` (добавить к существующим)

### Тест-кейсы:

6. **gracefulShutdown — отправляет ready в PM2**
   - Setup: `process.env.PM2_HOME = '/home/user/.pm2'`
   - Mock: `processObj.send` = spy
   - Ожидаем: `processObj.send` вызывается с `'ready'`

7. **gracefulShutdown — не отправляет ready без PM2**
   - Setup: `delete process.env.PM2_HOME; delete process.env.pm_id`
   - Mock: `processObj.send` = spy
   - Ожидаем: `processObj.send` НЕ вызывается

8. **gracefulShutdown — таймаут принудительного выхода**
   - Mock: все операции зависают (never resolve)
   - Ожидаем: `processObj.exit(1)` вызывается через ~30с

9. **registerShutdownHandlers — регистрирует SIGINT/SIGTERM/SIGUSR2**
   - Mock: `processObj.on` = spy
   - Ожидаем: `processObj.on` вызывается 3 раза с правильными сигналами

---

## Приоритет 3: realtimeService.js (system:updating notification)

**Файл**: `services/realtimeService.js`
**Тест**: `tests/realtimeService.test.js` (добавить к существующим)

### Тест-кейсы:

10. **shutdownClients — отправляет system:updating перед закрытием**
    - Setup: attachClient с mock res
    - Действие: shutdownClients()
    - Ожидаем: `res.write` вызывается с `event: system\ndata: {"type":"system:updating"...}`
    - Ожидаем: `res.end` вызывается после write

11. **shutdownClients — очищает keepAlive интервал**
    - Setup: attachClient с keepAlive
    - Действие: shutdownClients()
    - Ожидаем: clearInterval вызывается

---

## Приоритет 4: barberAliasService.js (edge cases)

**Файл**: `services/barberAliasService.js`
**Тест**: `tests/barberAliasService.test.js` (добавить к существующим)

### Тест-кейсы:

12. **registerBarberAlias — дубликат не сохраняется повторно**
    - Mock: `prisma.barberAliases.create` = spy
    - Действие: registerBarberAlias("b1", "Tim"); registerBarberAlias("b1", "Tim")
    - Ожидаем: `create` вызывается 1 раз

13. **loadBarberAliasesFromDisk — фильтрует пустые alias**
    - Mock: `prisma.barberAliases.findMany` возвращает `[{barberId:"b1", alias:""}, {barberId:"b1", alias:null}]`
    - Ожидаем: `getBarberAliases().get("b1")` = undefined

14. **loadBarberAliasesFromDisk — фильтрует alias с normalized пустым**
    - Mock: возвращает `[{barberId:"b1", alias:"   "}]`
    - Ожидаем: `getBarberAliases().get("b1")` = undefined

---

## Приоритет 5: homeDataService.js (blocked users error fallback)

**Файл**: `services/homeDataService.js`
**Тест**: `tests/homeDataService.test.js` (добавить к существующим)

### Тест-кейсы:

15. **readBlockedUsers — fallback на пустой Set при ошибке**
    - Mock: `prisma.blockedUsers.findMany` выбрасывает
    - Ожидаем: возвращает `new Set()`

---

## Критические файлы

| Файл | Действие |
|------|----------|
| `tests/botMenuService.test.js` | Создать новый |
| `tests/serverLifecycleService.test.js` | Добавить 4 теста |
| `tests/realtimeService.test.js` | Добавить 2 теста |
| `tests/barberAliasService.test.js` | Добавить 3 теста |
| `tests/homeDataService.test.js` | Добавить 1 тест |

---

## Верификация

```bash
node --test
```

Ожидаемый результат: 258 + ~15 новых = ~273 теста, все pass.
