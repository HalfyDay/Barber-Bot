const registerHomeRoutes = ({   app,   authenticateHomeToken,   prisma,   randomUUID,   normalizeText,   normalizePhone,   hashHomePassword,   verifyHomePassword,   findHomeUserByPhone,   findHomeUserByTelegramId,   findHomeUserById,   shouldHydrateUserNameFromHome,   HOME_USER_SELECT,   HOME_PROFILE_SELECT,   toPublicHomeUser,   toPublicHomeProfile,   buildHomeIdentity,   signHomeSessionToken,   buildLimitBlockedMessage,   getUserMeta,   updateUserMeta,   applyReferralCode,   buildReferralPayload,   resolveBsTransferRecipient,   transferBsBalance,   buildHomeAppPayload,   TELEGRAM_BOT_USERNAME,   markExpiredTelegramAuthRequests,   createTelegramAuthRequest,   getTelegramAuthRequestById,   updateTelegramAuthRequestById,   deleteTelegramAuthRequestById,   TELEGRAM_AUTH_FLOW_LOGIN,   TELEGRAM_AUTH_FLOW_PROFILE_LINK,   TELEGRAM_AUTH_STATUS_COMPLETED,   TELEGRAM_AUTH_STATUS_FAILED,   TELEGRAM_AUTH_STATUS_EXPIRED,   toTelegramIdNumber,   resolveHomeAssetPath,   getServiceCatalog,   resolveHomeBookingUser,   getHomeBookingSettings,   appointmentService,   getBarbers,   buildDateWindow,   STATUS_ACTIVE,   STATUS_CANCELLED,   notifyBarberAboutNewAppointment,   requestRealtimePush,   parseDateTime, }) => { const buildClientAccessPayload = async (phone) => { const safePhone = normalizePhone(phone); if (!safePhone) { return { isBarber: false, role: "client", barberId: "", barberName: "" }; } const barbers = await prisma.barbers.findMany({ where: { isActive: true, phone: { not: null } }, select: { id: true, name: true, phone: true, role: true } }); const matchedBarber = barbers.find((candidate) => normalizePhone(candidate.phone || "") === safePhone) || null; if (!matchedBarber) { return { isBarber: false, role: "client", barberId: "", barberName: "" }; } return { isBarber: true, role: normalizeText(matchedBarber.role) || "barber", barberId: normalizeText(matchedBarber.id), barberName: normalizeText(matchedBarber.name) }; };   app.post("/api/home/auth/register", async (req, res) => {     try {       const phoneInput = normalizeText(req.body?.phone);       const safePhone = normalizePhone(phoneInput);       const password = normalizeText(req.body?.password);       if (!safePhone || !password) {         return res.status(400).json({           success: false,           message: "Р’РІРµРґРёС‚Рµ РЅРѕРјРµСЂ С‚РµР»РµС„РѕРЅР° Рё РїР°СЂРѕР»СЊ.",         });       }       const displayName = normalizeText(req.body?.displayName) || safePhone;       const { hashHex, saltHex } = hashHomePassword(password);       const now = new Date().toISOString();       const existing = await findHomeUserByPhone(safePhone);       let row;       if (existing && normalizeText(existing.homePasswordHash)) {         return res.status(409).json({           success: false,           message: "РџРѕР»СЊР·РѕРІР°С‚РµР»СЊ СЃ С‚Р°РєРёРј РЅРѕРјРµСЂРѕРј СѓР¶Рµ Р·Р°СЂРµРіРёСЃС‚СЂРёСЂРѕРІР°РЅ.",         });       }       if (existing) {         const patch = {           Phone: safePhone,           homePasswordHash: hashHex,           homePasswordSalt: saltHex,           homeIsActive: true,           homeCreatedAt: existing.homeCreatedAt || now,           homeUpdatedAt: now,         };         if (shouldHydrateUserNameFromHome(existing.Name, existing.Phone)) {           patch.Name = displayName;         }         row = await prisma.users.update({           where: { id: existing.id },           data: patch,           select: HOME_USER_SELECT,         });       } else {         row = await prisma.users.create({           data: {             id: randomUUID(),             Name: displayName,             Phone: safePhone,             TelegramID: null,             Barber: null,             homePasswordHash: hashHex,             homePasswordSalt: saltHex,             homeIsActive: true,             homeCreatedAt: now,             homeUpdatedAt: now,             homeLastLoginAt: null,           },           select: HOME_USER_SELECT,         });       }       const user = toPublicHomeUser(row);       await applyReferralCode({         userId: user.id,         referralCode: req.body?.referralCode,       });       const identity = buildHomeIdentity({         userId: user.id,         phone: user.phone,         displayName: user.displayName,       });       const token = signHomeSessionToken(identity);       return res.status(201).json({         success: true,         token,         user,       });     } catch (error) {       if (         error?.code === "INVALID_PASSWORD" ||         error?.code === "INVALID_PHONE" ||         error?.code === "WEAK_PASSWORD"       ) {         return res.status(400).json({ success: false, message: error.message });       }       console.error("Home register error:", error);       return res         .status(500)         .json({ success: false, message: "РћС€РёР±РєР° СЂРµРіРёСЃС‚СЂР°С†РёРё. РџРѕРїСЂРѕР±СѓР№С‚Рµ РїРѕР·Р¶Рµ." });     }   });    app.post("/api/home/auth/login", async (req, res) => {     try {       const phoneInput = normalizeText(req.body?.phone);       const safePhone = normalizePhone(phoneInput);       const password = normalizeText(req.body?.password);       if (!safePhone || !password) {         return res.status(400).json({           success: false,           message: "?????????????? ?????????? ???????????????? ?? ????????????.",         });       }       let existing = await findHomeUserByPhone(safePhone);       let authenticatedViaBarberAccess = false;       if (         !existing ||         existing.homeIsActive === false ||         !normalizeText(existing.homePasswordHash) ||         !normalizeText(existing.homePasswordSalt) ||         !verifyHomePassword(password, existing.homePasswordHash, existing.homePasswordSalt)       ) {         const barbers = await prisma.barbers.findMany({           where: { isActive: true, phone: { not: null } },           select: { phone: true, password: true },         });         const matchingBarber = barbers.find((candidate) => normalizePhone(candidate.phone || "") === safePhone) || null;         if (!matchingBarber || normalizeText(matchingBarber.password) !== password) {           return res.status(401).json({             success: false,             message: "???????????????? ?????????? ???????????????? ?????? ????????????.",           });         }         const users = await prisma.users.findMany({           where: { Phone: { not: null } },           select: HOME_USER_SELECT,         });         existing = users.find((entry) => normalizePhone(entry.Phone || "") === safePhone) || null;         if (!existing) {           return res.status(409).json({             success: false,             message: "??? ????? ??????? ?? ???? ????? ?????????? ??????? ? ??? ?? ??????? ????????.",           });         }         authenticatedViaBarberAccess = true;       }       const now = new Date().toISOString();       const updated = await prisma.users.update({         where: { id: existing.id },         data: {           homeIsActive: true,           homeLastLoginAt: now,           homeUpdatedAt: now,         },         select: HOME_USER_SELECT,       });       const user = toPublicHomeUser(updated);       const access = await buildClientAccessPayload(user.phone);       const identity = buildHomeIdentity({         userId: user.id,         phone: user.phone,         displayName: user.displayName,         accountRole: access.role,         barberId: access.barberId,         barberName: access.barberName,         isBarber: access.isBarber,       });       const token = signHomeSessionToken(identity);       return res.json({         success: true,         token,         user: {           ...user,           access,           authenticatedViaBarberAccess,         },       });     } catch (error) {       console.error("Home login error:", error);       return res         .status(500)         .json({ success: false, message: "???????????? ??????????. ???????????????????? ??????????." });     }   });    app.post("/api/home/auth/telegram/start", async (req, res) => {     try {       await markExpiredTelegramAuthRequests();       const request = await createTelegramAuthRequest({         flow: TELEGRAM_AUTH_FLOW_LOGIN,       });       const botLink = TELEGRAM_BOT_USERNAME         ? `https://t.me/${encodeURIComponent(             TELEGRAM_BOT_USERNAME,           )}?start=${encodeURIComponent(`site_login_${request.code}`)}`         : null;       return res.status(201).json({         success: true,         requestId: request.id,         code: request.code,         command: `/site_login ${request.code}`,         expiresAt: request.expiresAt,         botLink,         botUsername: TELEGRAM_BOT_USERNAME || null,       });     } catch (error) {       console.error("Home telegram auth start error:", error);       return res.status(500).json({         success: false,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РїСѓСЃС‚РёС‚СЊ Р°РІС‚РѕСЂРёР·Р°С†РёСЋ С‡РµСЂРµР· Telegram.",       });     }   });    app.get("/api/home/auth/telegram/status", async (req, res) => {     const requestId = normalizeText(req.query?.requestId);     if (!requestId) {       return res.status(400).json({         success: false,         done: true,         message: "РЈРєР°Р¶РёС‚Рµ requestId.",       });     }     try {       await markExpiredTelegramAuthRequests();       const row = await getTelegramAuthRequestById(requestId);       if (!row) {         return res.status(404).json({           success: false,           done: true,           message: "РЎРµСЃСЃРёСЏ Telegram Р°РІС‚РѕСЂРёР·Р°С†РёРё РЅРµ РЅР°Р№РґРµРЅР° РёР»Рё СѓР¶Рµ Р·Р°РІРµСЂС€РµРЅР°.",         });       }       if (         normalizeText(row.flow || TELEGRAM_AUTH_FLOW_LOGIN) !==         TELEGRAM_AUTH_FLOW_LOGIN       ) {         return res.status(409).json({           success: false,           done: true,           message: "Р­С‚РѕС‚ Р·Р°РїСЂРѕСЃ РїСЂРµРґРЅР°Р·РЅР°С‡РµРЅ РґР»СЏ РґСЂСѓРіРѕРіРѕ СЃС†РµРЅР°СЂРёСЏ Telegram.",         });       }       if (row.status === TELEGRAM_AUTH_STATUS_COMPLETED) {         const rowUserId = normalizeText(row.userId);         let userRow = rowUserId           ? await prisma.users.findUnique({               where: { id: rowUserId },               select: { ...HOME_USER_SELECT, TelegramID: true },             })           : null;         const safeTelegramId = normalizeText(row.telegramId);         if (!userRow && safeTelegramId) {           userRow = await findHomeUserByTelegramId(safeTelegramId);         }         const safePhone = normalizePhone(row.phone || userRow?.Phone || "");         if (!userRow && safePhone) {           userRow = await findHomeUserByPhone(safePhone);         }         const resolvedUserId = normalizeText(userRow?.id);         const resolvedDisplayName =           normalizeText(row.displayName || userRow?.Name || "") || null;         if (           resolvedUserId &&           (resolvedUserId !== rowUserId ||             safePhone !== normalizePhone(row.phone || "") ||             resolvedDisplayName !== normalizeText(row.displayName || ""))         ) {           await updateTelegramAuthRequestById(requestId, {             userId: resolvedUserId,             phone: safePhone || null,             displayName: resolvedDisplayName,             errorMessage: null,           });         }         const hasPassword = Boolean(           userRow &&             normalizeText(userRow.homePasswordHash) &&             normalizeText(userRow.homePasswordSalt),         );         if (userRow && userRow.homeIsActive !== false && hasPassword && safePhone) {           const now = new Date().toISOString();           const patch = {             Phone: safePhone,             homeIsActive: true,             homeLastLoginAt: now,             homeUpdatedAt: now,           };           const telegramIdAsNumber = toTelegramIdNumber(row.telegramId);           if (telegramIdAsNumber !== null) {             patch.TelegramID = telegramIdAsNumber;           }           if (             resolvedDisplayName &&             shouldHydrateUserNameFromHome(userRow.Name, userRow.Phone)           ) {             patch.Name = resolvedDisplayName;           }           const updated = await prisma.users.update({             where: { id: userRow.id },             data: patch,             select: HOME_USER_SELECT,           });           const user = toPublicHomeUser(updated);           const identity = buildHomeIdentity({             userId: user.id,             phone: user.phone,             displayName: user.displayName,           });           const token = signHomeSessionToken(identity);           try {             await deleteTelegramAuthRequestById(requestId);           } catch (cleanupError) {             console.warn(               "Telegram auth request cleanup warning:",               cleanupError?.message || cleanupError,             );           }           return res.json({             success: true,             done: true,             token,             user,           });         }         if (!safePhone) {           return res.status(409).json({             success: false,             done: true,             message:               "Р”Р»СЏ РІС…РѕРґР° РЅСѓР¶РµРЅ РїРѕРґС‚РІРµСЂР¶РґРµРЅРЅС‹Р№ РЅРѕРјРµСЂ С‚РµР»РµС„РѕРЅР° РІ Telegram. РќР°Р¶РјРёС‚Рµ РІС…РѕРґ С‡РµСЂРµР· Telegram СЃРЅРѕРІР°.",           });         }         return res.json({           success: true,           done: true,           needsSetup: true,           requestId,           setupMode: userRow ? "set_password" : "register",           phone: safePhone,           displayName: resolvedDisplayName,         });       }       if (         row.status === TELEGRAM_AUTH_STATUS_FAILED ||         row.status === TELEGRAM_AUTH_STATUS_EXPIRED       ) {         return res.json({           success: false,           done: true,           status: row.status,           message:             normalizeText(row.errorMessage) ||             (row.status === TELEGRAM_AUTH_STATUS_EXPIRED               ? "РљРѕРґ Telegram Р°РІС‚РѕСЂРёР·Р°С†РёРё РёСЃС‚РµРє. Р—Р°РїСЂРѕСЃРёС‚Рµ РЅРѕРІС‹Р№."               : "Telegram Р°РІС‚РѕСЂРёР·Р°С†РёСЏ Р·Р°РІРµСЂС€РёР»Р°СЃСЊ СЃ РѕС€РёР±РєРѕР№."),         });       }       return res.json({         success: true,         done: false,         status: row.status,         expiresAt: row.expiresAt || null,         updatedAt: row.updatedAt || null,       });     } catch (error) {       console.error("Home telegram auth status error:", error);       return res.status(500).json({         success: false,         done: true,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ РїСЂРѕРІРµСЂРёС‚СЊ Telegram Р°РІС‚РѕСЂРёР·Р°С†РёСЋ.",       });     }   });    app.post("/api/home/auth/telegram/complete", async (req, res) => {     const requestId = normalizeText(req.body?.requestId);     const displayNameInput = normalizeText(req.body?.displayName);     const phoneInput = normalizeText(req.body?.phone);     const password = normalizeText(req.body?.password);     if (!requestId || !password) {       return res.status(400).json({         success: false,         message: "РќРµРґРѕСЃС‚Р°С‚РѕС‡РЅРѕ РґР°РЅРЅС‹С… РґР»СЏ Р·Р°РІРµСЂС€РµРЅРёСЏ Telegram-РІС…РѕРґР°.",       });     }     try {       await markExpiredTelegramAuthRequests();       const row = await getTelegramAuthRequestById(requestId);       if (!row) {         return res.status(404).json({           success: false,           message: "РЎРµСЃСЃРёСЏ Telegram Р°РІС‚РѕСЂРёР·Р°С†РёРё РЅРµ РЅР°Р№РґРµРЅР°.",         });       }       if (         normalizeText(row.flow || TELEGRAM_AUTH_FLOW_LOGIN) !==         TELEGRAM_AUTH_FLOW_LOGIN       ) {         return res.status(409).json({           success: false,           message: "Р­С‚РѕС‚ Р·Р°РїСЂРѕСЃ Telegram РЅРµ РїСЂРµРґРЅР°Р·РЅР°С‡РµРЅ РґР»СЏ Р·Р°РІРµСЂС€РµРЅРёСЏ РІС…РѕРґР°.",         });       }       if (row.status !== TELEGRAM_AUTH_STATUS_COMPLETED) {         return res.status(409).json({           success: false,           message:             row.status === TELEGRAM_AUTH_STATUS_EXPIRED               ? "РљРѕРґ Telegram Р°РІС‚РѕСЂРёР·Р°С†РёРё РёСЃС‚РµРє. Р—Р°РїСЂРѕСЃРёС‚Рµ РЅРѕРІС‹Р№."               : "Telegram Р°РІС‚РѕСЂРёР·Р°С†РёСЏ РµС‰Рµ РЅРµ Р·Р°РІРµСЂС€РµРЅР°.",         });       }       const safeTelegramId = normalizeText(row.telegramId);       const telegramIdAsNumber = toTelegramIdNumber(safeTelegramId);       let userRow = null;       const rowUserId = normalizeText(row.userId);       if (rowUserId) {         userRow = await prisma.users.findUnique({           where: { id: rowUserId },           select: { ...HOME_USER_SELECT, TelegramID: true },         });       }       if (!userRow && safeTelegramId) {         userRow = await findHomeUserByTelegramId(safeTelegramId);       }       const safePhone = normalizePhone(phoneInput || row.phone || userRow?.Phone || "");       if (!safePhone) {         return res.status(409).json({           success: false,           message:             "РўРµР»РµС„РѕРЅ РЅРµ РїРѕРґС‚РІРµСЂР¶РґРµРЅ. РќР°Р¶РјРёС‚Рµ РІС…РѕРґ С‡РµСЂРµР· Telegram СЃРЅРѕРІР° Рё РѕС‚РїСЂР°РІСЊС‚Рµ РєРѕРЅС‚Р°РєС‚.",         });       }       const phoneMatches = await prisma.users.findMany({         where: { Phone: { not: null } },         select: {           id: true,           Phone: true,           homePasswordHash: true,           homePasswordSalt: true,           homeIsActive: true,         },       });       const conflict = phoneMatches.find(         (entry) =>           entry.id !== userRow?.id &&           normalizePhone(entry.Phone || "") === safePhone &&           entry.homeIsActive !== false &&           normalizeText(entry.homePasswordHash) &&           normalizeText(entry.homePasswordSalt),       );       if (conflict) {         return res.status(409).json({           success: false,           message: "Р­С‚РѕС‚ РЅРѕРјРµСЂ СѓР¶Рµ РёСЃРїРѕР»СЊР·СѓРµС‚СЃСЏ РґСЂСѓРіРёРј Р°РєРєР°СѓРЅС‚РѕРј.",         });       }       const { hashHex, saltHex } = hashHomePassword(password);       const now = new Date().toISOString();       let persisted;       if (userRow) {         const nextDisplayName =           displayNameInput || normalizeText(row.displayName || userRow.Name || "");         const patch = {           Phone: safePhone,           homePasswordHash: hashHex,           homePasswordSalt: saltHex,           homeIsActive: true,           homeCreatedAt: userRow.homeCreatedAt || now,           homeUpdatedAt: now,           homeLastLoginAt: now,         };         if (telegramIdAsNumber !== null) {           patch.TelegramID = telegramIdAsNumber;         }         if (           nextDisplayName &&           shouldHydrateUserNameFromHome(userRow.Name, userRow.Phone)         ) {           patch.Name = nextDisplayName;         }         persisted = await prisma.users.update({           where: { id: userRow.id },           data: patch,           select: HOME_USER_SELECT,         });         await updateTelegramAuthRequestById(requestId, {           userId: userRow.id,           phone: safePhone,           displayName: nextDisplayName || null,           errorMessage: null,         });       } else {         const nextDisplayName = displayNameInput;         if (!nextDisplayName) {           return res.status(400).json({             success: false,             message: "Р’РІРµРґРёС‚Рµ Р¤РРћ РґР»СЏ РЅРѕРІРѕРіРѕ Р°РєРєР°СѓРЅС‚Р°.",           });         }         persisted = await prisma.users.create({           data: {             id: randomUUID(),             Name: nextDisplayName,             Phone: safePhone,             TelegramID: telegramIdAsNumber,             Barber: null,             homePasswordHash: hashHex,             homePasswordSalt: saltHex,             homeIsActive: true,             homeCreatedAt: now,             homeUpdatedAt: now,             homeLastLoginAt: now,           },           select: HOME_USER_SELECT,         });         await updateTelegramAuthRequestById(requestId, {           userId: persisted.id,           phone: safePhone,           displayName: nextDisplayName,           errorMessage: null,         });       }       const user = toPublicHomeUser(persisted);       await applyReferralCode({         userId: user.id,         referralCode: req.body?.referralCode,       });       const identity = buildHomeIdentity({         userId: user.id,         phone: user.phone,         displayName: user.displayName,       });       const token = signHomeSessionToken(identity);       try {         await deleteTelegramAuthRequestById(requestId);       } catch (cleanupError) {         console.warn(           "Telegram auth request cleanup warning:",           cleanupError?.message || cleanupError,         );       }       return res.json({         success: true,         token,         user,       });     } catch (error) {       if (         error?.code === "INVALID_PASSWORD" ||         error?.code === "WEAK_PASSWORD" ||         error?.code === "INVALID_PHONE"       ) {         return res.status(400).json({ success: false, message: error.message });       }       console.error("Home telegram auth complete error:", error);       return res.status(500).json({         success: false,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РІРµСЂС€РёС‚СЊ РІС…РѕРґ С‡РµСЂРµР· Telegram.",       });     }   });    app.get("/api/home/auth/me", authenticateHomeToken, async (req, res) => {     const identity = req.homeUser || {};     const stored = await findHomeUserById(identity.userId);     if (!stored) return res.sendStatus(401);     const user = toPublicHomeUser(stored);     return res.json({       authenticated: true,       user: {         ...user,         access: {           isBarber: identity.isBarber === true,           role: normalizeText(identity.accountRole) || "client",           barberId: normalizeText(identity.barberId),           barberName: normalizeText(identity.barberName),         },       },     });   });    app.get("/api/home/profile", authenticateHomeToken, async (req, res) => {     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       const stored = await prisma.users.findUnique({         where: { id: userId },         select: HOME_PROFILE_SELECT,       });       if (         !stored ||         stored.homeIsActive === false ||         !normalizeText(stored.homePasswordHash) ||         !normalizeText(stored.homePasswordSalt)       ) {         return res.sendStatus(401);       }       const meta = await getUserMeta(userId);       return res.json({         success: true,         user: {           ...toPublicHomeProfile(stored),           birthDate: meta?.birthDate || null,           gender: meta?.gender || "",           avatarUrl: meta?.avatarUrl || "",           bookingNotificationsEnabled: meta?.bookingNotificationsEnabled !== false,           referralCode: meta?.referralCode || "",         },         botUsername: TELEGRAM_BOT_USERNAME || null,       });     } catch (error) {       console.error("Home profile read error:", error);       return res.status(500).json({         success: false,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РіСЂСѓР·РёС‚СЊ РїСЂРѕС„РёР»СЊ.",       });     }   });    app.put("/api/home/profile", authenticateHomeToken, async (req, res) => {     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       const current = await prisma.users.findUnique({         where: { id: userId },         select: HOME_PROFILE_SELECT,       });       if (         !current ||         current.homeIsActive === false ||         !normalizeText(current.homePasswordHash) ||         !normalizeText(current.homePasswordSalt)       ) {         return res.sendStatus(401);       }        const displayNameInput = normalizeText(req.body?.displayName);       const phoneInput = normalizeText(req.body?.phone);       const passwordInput = normalizeText(req.body?.password);       const safePhone = normalizePhone(phoneInput || current.Phone || "");       if (!safePhone) {         return res.status(400).json({           success: false,           message: "Р’РІРµРґРёС‚Рµ РєРѕСЂСЂРµРєС‚РЅС‹Р№ РЅРѕРјРµСЂ С‚РµР»РµС„РѕРЅР°.",         });       }        const currentPhone = normalizePhone(current.Phone || "");       if (safePhone !== currentPhone) {         const phoneMatches = await prisma.users.findMany({           where: { Phone: { not: null } },           select: {             id: true,             Phone: true,             homePasswordHash: true,             homePasswordSalt: true,             homeIsActive: true,           },         });         const conflict = phoneMatches.find(           (entry) =>             entry.id !== userId &&             normalizePhone(entry.Phone || "") === safePhone &&             entry.homeIsActive !== false &&             normalizeText(entry.homePasswordHash) &&             normalizeText(entry.homePasswordSalt),         );         if (conflict) {           return res.status(409).json({             success: false,             message: "Р­С‚РѕС‚ РЅРѕРјРµСЂ СѓР¶Рµ РёСЃРїРѕР»СЊР·СѓРµС‚СЃСЏ РґСЂСѓРіРёРј Р°РєРєР°СѓРЅС‚РѕРј.",           });         }       }        const currentDisplayName = normalizeText(current.Name || current.Phone || safePhone);       const nextDisplayName = displayNameInput || currentDisplayName;       const nameChanged = nextDisplayName !== currentDisplayName;       const phoneChanged = safePhone !== currentPhone;       const limits = toPublicHomeProfile(current)?.limits || {};       if (nameChanged && limits?.name?.isLocked) {         return res.status(429).json({           success: false,           message: buildLimitBlockedMessage("Р¤РРћ", limits.name),         });       }       if (phoneChanged && limits?.phone?.isLocked) {         return res.status(429).json({           success: false,           message: buildLimitBlockedMessage("РўРµР»РµС„РѕРЅ", limits.phone),         });       }        const now = new Date().toISOString();       const patch = {         Name: nextDisplayName,         Phone: safePhone,         homeIsActive: true,         homeCreatedAt: current.homeCreatedAt || now,         homeUpdatedAt: now,       };       if (nameChanged) {         patch.LastNameChanged = now;       }       if (phoneChanged) {         patch.homePhoneChangedAt = now;       }       if (passwordInput) {         const { hashHex, saltHex } = hashHomePassword(passwordInput);         patch.homePasswordHash = hashHex;         patch.homePasswordSalt = saltHex;       }       const birthDate = normalizeText(req.body?.birthDate);       const gender = normalizeText(req.body?.gender);       const avatarUrl = normalizeText(req.body?.avatarUrl);       const bookingNotificationsEnabledRaw = req.body?.bookingNotificationsEnabled;       const bookingNotificationsEnabled =         bookingNotificationsEnabledRaw === undefined           ? undefined           : !["false", "0", "off", ""].includes(               String(bookingNotificationsEnabledRaw).trim().toLowerCase(),             );        const updated = await prisma.users.update({         where: { id: userId },         data: patch,         select: HOME_PROFILE_SELECT,       });       const currentMeta = await getUserMeta(userId);       const meta = await updateUserMeta(userId, {         birthDate: birthDate || null,         gender: gender || "",         avatarUrl: avatarUrl || currentMeta?.avatarUrl || "",         ...(bookingNotificationsEnabled === undefined ? {} : { bookingNotificationsEnabled }),       });       const user = toPublicHomeProfile(updated);       const identity = buildHomeIdentity({         userId: user.id,         phone: user.phone,         displayName: user.displayName,       });       const token = signHomeSessionToken(identity);       res.setHeader("x-home-session-token", token);       return res.json({         success: true,         token,         user: {           ...user,           birthDate: meta?.birthDate || null,           gender: meta?.gender || "",           avatarUrl: meta?.avatarUrl || "",           bookingNotificationsEnabled: meta?.bookingNotificationsEnabled !== false,           referralCode: meta?.referralCode || "",         },       });     } catch (error) {       if (         error?.code === "INVALID_PASSWORD" ||         error?.code === "INVALID_PHONE" ||         error?.code === "WEAK_PASSWORD"       ) {         return res.status(400).json({ success: false, message: error.message });       }       console.error("Home profile update error:", error);       return res.status(500).json({         success: false,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ СЃРѕС…СЂР°РЅРёС‚СЊ РёР·РјРµРЅРµРЅРёСЏ РїСЂРѕС„РёР»СЏ.",       });     }   });    app.get("/api/home/app", authenticateHomeToken, async (req, res) => {     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       const payload = await buildHomeAppPayload(userId);       if (!payload) return res.sendStatus(401);       return res.json({         success: true,         ...payload,         botUsername: TELEGRAM_BOT_USERNAME || null,       });     } catch (error) {       console.error("Home app payload error:", {         userId: normalizeText(req.homeUser?.userId),         phone: normalizeText(req.homeUser?.phone),         message: error?.message || error,         stack: error?.stack || null,       });       return res.status(500).json({         success: false,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РіСЂСѓР·РёС‚СЊ РґР°РЅРЅС‹Рµ РїСЂРёР»РѕР¶РµРЅРёСЏ.",       });     }   });    app.get("/api/home/referral", authenticateHomeToken, async (req, res) => {     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       const stored = await prisma.users.findUnique({ where: { id: userId } });       if (!stored) return res.sendStatus(401);       const referral = await buildReferralPayload(stored);       return res.json({         success: true,         referral,       });     } catch (error) {       console.error("Home referral payload error:", error);       return res.status(500).json({         success: false,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РіСЂСѓР·РёС‚СЊ СЂРµС„РµСЂР°Р»СЊРЅСѓСЋ СЃРёСЃС‚РµРјСѓ.",       });     }   });    app.post("/api/home/referral/transfer/preview", authenticateHomeToken, async (req, res) => {     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       const recipient = await resolveBsTransferRecipient({         fromUserId: userId,         targetPhone: req.body?.targetPhone,       });       return res.json({         success: true,         recipient,       });     } catch (error) {       const messageMap = {         TARGET_REQUIRED: "РЈРєР°Р¶РёС‚Рµ С‚РµР»РµС„РѕРЅ РїРѕР»СѓС‡Р°С‚РµР»СЏ.",         TARGET_NOT_FOUND: "РџРѕР»СѓС‡Р°С‚РµР»СЊ РЅРµ РЅР°Р№РґРµРЅ.",         SELF_TRANSFER: "РќРµР»СЊР·СЏ РїРµСЂРµРІРѕРґРёС‚СЊ BS СЃР°РјРѕРјСѓ СЃРµР±Рµ.",         UNAUTHORIZED: "РќРµ СѓРґР°Р»РѕСЃСЊ РїРѕРґС‚РІРµСЂРґРёС‚СЊ РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ.",       };       const code = normalizeText(error?.message);       return res.status(400).json({         success: false,         message: messageMap[code] || "РќРµ СѓРґР°Р»РѕСЃСЊ РїСЂРѕРІРµСЂРёС‚СЊ РїРѕР»СѓС‡Р°С‚РµР»СЏ.",       });     }   });    app.post("/api/home/referral/transfer", authenticateHomeToken, async (req, res) => {     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       const stored = await prisma.users.findUnique({ where: { id: userId } });       if (!stored) return res.sendStatus(401);        const result = await transferBsBalance({         fromUserId: userId,         targetPhone: req.body?.targetPhone,         amountBs: req.body?.amountBs,         comment: req.body?.comment,       });       const referral = await buildReferralPayload(stored);       return res.json({         success: true,         referral,         transfer: result,       });     } catch (error) {       const messageMap = {         TARGET_REQUIRED: "РЈРєР°Р¶РёС‚Рµ С‚РµР»РµС„РѕРЅ РїРѕР»СѓС‡Р°С‚РµР»СЏ.",         INVALID_AMOUNT: "РЈРєР°Р¶РёС‚Рµ РєРѕСЂСЂРµРєС‚РЅСѓСЋ СЃСѓРјРјСѓ РїРµСЂРµРІРѕРґР°.",         TARGET_NOT_FOUND: "РџРѕР»СѓС‡Р°С‚РµР»СЊ РЅРµ РЅР°Р№РґРµРЅ.",         SELF_TRANSFER: "РќРµР»СЊР·СЏ РїРµСЂРµРІРѕРґРёС‚СЊ BS СЃР°РјРѕРјСѓ СЃРµР±Рµ.",         INSUFFICIENT_BS: "РќРµРґРѕСЃС‚Р°С‚РѕС‡РЅРѕ BS РґР»СЏ РїРµСЂРµРІРѕРґР°.",         UNAUTHORIZED: "РќРµ СѓРґР°Р»РѕСЃСЊ РїРѕРґС‚РІРµСЂРґРёС‚СЊ РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ.",       };       const code = normalizeText(error?.message);       return res.status(400).json({         success: false,         message: messageMap[code] || "РќРµ СѓРґР°Р»РѕСЃСЊ РІС‹РїРѕР»РЅРёС‚СЊ РїРµСЂРµРІРѕРґ BS.",       });     }   });    app.post("/api/home/profile/telegram/start", authenticateHomeToken, async (req, res) => {     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       const current = await prisma.users.findUnique({         where: { id: userId },         select: HOME_PROFILE_SELECT,       });       if (         !current ||         current.homeIsActive === false ||         !normalizeText(current.homePasswordHash) ||         !normalizeText(current.homePasswordSalt)       ) {         return res.sendStatus(401);       }       if (normalizeText(current.TelegramID)) {         return res.status(409).json({           success: false,           message: "Telegram СѓР¶Рµ РїСЂРёРІСЏР·Р°РЅ Рє СЌС‚РѕРјСѓ Р°РєРєР°СѓРЅС‚Сѓ.",         });       }       await markExpiredTelegramAuthRequests();       const request = await createTelegramAuthRequest({         flow: TELEGRAM_AUTH_FLOW_PROFILE_LINK,         targetUserId: userId,       });       const botLink = TELEGRAM_BOT_USERNAME         ? `https://t.me/${encodeURIComponent(             TELEGRAM_BOT_USERNAME,           )}?start=${encodeURIComponent(`site_login_${request.code}`)}`         : null;       return res.status(201).json({         success: true,         requestId: request.id,         code: request.code,         command: `/site_login ${request.code}`,         expiresAt: request.expiresAt,         botLink,         botUsername: TELEGRAM_BOT_USERNAME || null,       });     } catch (error) {       console.error("Home profile telegram start error:", error);       return res.status(500).json({         success: false,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РїСѓСЃС‚РёС‚СЊ РїСЂРёРІСЏР·РєСѓ Telegram.",       });     }   });    app.get("/api/home/profile/telegram/status", authenticateHomeToken, async (req, res) => {     const requestId = normalizeText(req.query?.requestId);     if (!requestId) {       return res.status(400).json({         success: false,         done: true,         message: "РЈРєР°Р¶РёС‚Рµ requestId.",       });     }     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       await markExpiredTelegramAuthRequests();       const row = await getTelegramAuthRequestById(requestId);       if (!row) {         return res.status(404).json({           success: false,           done: true,           message: "РЎРµСЃСЃРёСЏ РїСЂРёРІСЏР·РєРё Telegram РЅРµ РЅР°Р№РґРµРЅР°.",         });       }       if (         normalizeText(row.flow || TELEGRAM_AUTH_FLOW_LOGIN) !==         TELEGRAM_AUTH_FLOW_PROFILE_LINK       ) {         return res.status(409).json({           success: false,           done: true,           message: "Р­С‚РѕС‚ Р·Р°РїСЂРѕСЃ Telegram РѕС‚РЅРѕСЃРёС‚СЃСЏ Рє РґСЂСѓРіРѕРјСѓ СЃС†РµРЅР°СЂРёСЋ.",         });       }       if (normalizeText(row.targetUserId) !== userId) {         return res.status(403).json({           success: false,           done: true,           message: "Р­С‚РѕС‚ Р·Р°РїСЂРѕСЃ РїСЂРёРІСЏР·РєРё СЃРѕР·РґР°РЅ РґР»СЏ РґСЂСѓРіРѕРіРѕ РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ.",         });       }       if (row.status === TELEGRAM_AUTH_STATUS_COMPLETED) {         const safeTelegramId = normalizeText(row.telegramId);         if (!safeTelegramId) {           await updateTelegramAuthRequestById(requestId, {             status: TELEGRAM_AUTH_STATUS_FAILED,             errorMessage: "Telegram ID РЅРµ РїРѕР»СѓС‡РµРЅ. Р—Р°РїСѓСЃС‚РёС‚Рµ РїСЂРёРІСЏР·РєСѓ Р·Р°РЅРѕРІРѕ.",           });           return res.status(409).json({             success: false,             done: true,             message: "Telegram ID РЅРµ РїРѕР»СѓС‡РµРЅ. Р—Р°РїСѓСЃС‚РёС‚Рµ РїСЂРёРІСЏР·РєСѓ Р·Р°РЅРѕРІРѕ.",           });         }         const telegramIdAsNumber = toTelegramIdNumber(safeTelegramId);         if (telegramIdAsNumber === null) {           await updateTelegramAuthRequestById(requestId, {             status: TELEGRAM_AUTH_STATUS_FAILED,             errorMessage: "РќРµРєРѕСЂСЂРµРєС‚РЅС‹Р№ Telegram ID.",           });           return res.status(409).json({             success: false,             done: true,             message: "РќРµРєРѕСЂСЂРµРєС‚РЅС‹Р№ Telegram ID. Р—Р°РїСѓСЃС‚РёС‚Рµ РїСЂРёРІСЏР·РєСѓ Р·Р°РЅРѕРІРѕ.",           });         }         const current = await prisma.users.findUnique({           where: { id: userId },           select: HOME_PROFILE_SELECT,         });         if (           !current ||           current.homeIsActive === false ||           !normalizeText(current.homePasswordHash) ||           !normalizeText(current.homePasswordSalt)         ) {           return res.sendStatus(401);         }         const linkedUser = await findHomeUserByTelegramId(safeTelegramId);         if (linkedUser && linkedUser.id !== userId) {           await updateTelegramAuthRequestById(requestId, {             status: TELEGRAM_AUTH_STATUS_FAILED,             errorMessage: "Р­С‚РѕС‚ Telegram СѓР¶Рµ РїСЂРёРІСЏР·Р°РЅ Рє РґСЂСѓРіРѕРјСѓ Р°РєРєР°СѓРЅС‚Сѓ.",           });           return res.json({             success: false,             done: true,             message: "Р­С‚РѕС‚ Telegram СѓР¶Рµ РїСЂРёРІСЏР·Р°РЅ Рє РґСЂСѓРіРѕРјСѓ Р°РєРєР°СѓРЅС‚Сѓ.",           });         }         const updated = await prisma.users.update({           where: { id: userId },           data: {             TelegramID: telegramIdAsNumber,             homeUpdatedAt: new Date().toISOString(),             homeTelegramChangedAt: new Date().toISOString(),           },           select: HOME_PROFILE_SELECT,         });         try {             await deleteTelegramAuthRequestById(requestId);         } catch (cleanupError) {           console.warn(             "Telegram profile link request cleanup warning:",             cleanupError?.message || cleanupError,           );         }         return res.json({           success: true,           done: true,           user: toPublicHomeProfile(updated),         });       }       if (         row.status === TELEGRAM_AUTH_STATUS_FAILED ||         row.status === TELEGRAM_AUTH_STATUS_EXPIRED       ) {         return res.json({           success: false,           done: true,           status: row.status,           message:             normalizeText(row.errorMessage) ||             (row.status === TELEGRAM_AUTH_STATUS_EXPIRED               ? "РљРѕРґ РїСЂРёРІСЏР·РєРё Telegram РёСЃС‚РµРє. Р—Р°РїСЂРѕСЃРёС‚Рµ РЅРѕРІС‹Р№."               : "РџСЂРёРІСЏР·РєР° Telegram Р·Р°РІРµСЂС€РёР»Р°СЃСЊ СЃ РѕС€РёР±РєРѕР№."),         });       }       return res.json({         success: true,         done: false,         status: row.status,         expiresAt: row.expiresAt || null,         updatedAt: row.updatedAt || null,       });     } catch (error) {       console.error("Home profile telegram status error:", error);       return res.status(500).json({         success: false,         done: true,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ РїСЂРѕРІРµСЂРёС‚СЊ РїСЂРёРІСЏР·РєСѓ Telegram.",       });     }   });    app.post("/api/home/profile/telegram/unlink", authenticateHomeToken, async (req, res) => {     try {       const userId = normalizeText(req.homeUser?.userId);       if (!userId) return res.sendStatus(401);       const current = await prisma.users.findUnique({         where: { id: userId },         select: HOME_PROFILE_SELECT,       });       if (         !current ||         current.homeIsActive === false ||         !normalizeText(current.homePasswordHash) ||         !normalizeText(current.homePasswordSalt)       ) {         return res.sendStatus(401);       }       const updated = await prisma.users.update({         where: { id: userId },         data: {           TelegramID: null,           homeUpdatedAt: new Date().toISOString(),           homeTelegramChangedAt: null,         },         select: HOME_PROFILE_SELECT,       });       return res.json({         success: true,         user: toPublicHomeProfile(updated),       });     } catch (error) {       console.error("Home profile telegram unlink error:", error);       return res.status(500).json({         success: false,         message: "РќРµ СѓРґР°Р»РѕСЃСЊ РѕС‚РІСЏР·Р°С‚СЊ Telegram.",       });     }   });    app.get("/api/home/barbers", authenticateHomeToken, async (req, res) => {     try {       const [barbers, servicesCatalog] = await Promise.all([         prisma.barbers.findMany({           where: { isActive: true },           select: {             id: true,             name: true,             nickname: true,             description: true,             color: true,             rating: true,             orderIndex: true,             avatarUrl: true,             cardImageUrl: true,             cardTitle: true,             cardPhrase: true,           },           orderBy: [{ orderIndex: "asc" }, { name: "asc" }],         }),         getServiceCatalog(false),       ]);       const getBarberServicesCount = (barberId) =>         servicesCatalog.reduce((count, service) => {           const rawPrice = service?.prices?.[barberId] ?? service?.prices?.[String(barberId)];           const price = Number(rawPrice);           return Number.isFinite(price) && price > 0 ? count + 1 : count;         }, 0);       const barbersWithServices = barbers         .map((barber) => ({           ...barber,           servicesCount: getBarberServicesCount(barber.id),         }))         .filter((barber) => barber.servicesCount > 0);       const fallbackImage = "/Image/card/Barber_photo.png";       const payload = barbersWithServices.map((barber) => {         const displayName =           normalizeText(barber.cardTitle) ||           normalizeText(barber.nickname) ||           normalizeText(barber.name) ||           "Р‘Р°СЂР±РµСЂ";         const cardImage = resolveHomeAssetPath(barber.cardImageUrl);         const avatarImage = resolveHomeAssetPath(barber.avatarUrl);         return {           id: barber.id,           name: displayName,           fullName: normalizeText(barber.name) || displayName,           description: normalizeText(barber.description),           color: normalizeText(barber.color) || "#17c8c0",           rating: normalizeText(barber.rating),           originalImageUrl: avatarImage || cardImage || fallbackImage,           imageUrl: cardImage || avatarImage || fallbackImage,           thumbnailUrl: avatarImage || cardImage || fallbackImage,           phrase: normalizeText(barber.cardPhrase) || normalizeText(barber.description),           servicesCount: barber.servicesCount,         };       });       return res.json({ barbers: payload });     } catch (error) {       console.error("Home barbers load error:", error);       return res.status(500).json({ error: "РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РіСЂСѓР·РёС‚СЊ СЃРїРёСЃРѕРє Р±Р°СЂР±РµСЂРѕРІ." });     }   });    app.get("/api/home/booking/services", authenticateHomeToken, async (req, res) => {     try {       const homeUser = await resolveHomeBookingUser(req);       if (!homeUser) return res.sendStatus(401);       const barberId = normalizeText(req.query?.barberId);       if (!barberId) {         return res.status(400).json({ error: "РЈРєР°Р¶РёС‚Рµ barberId РІС‹Р±СЂР°РЅРЅРѕРіРѕ Р±Р°СЂР±РµСЂР°." });       }        const [settings, activeAppointments, barbers, servicesCatalog] = await Promise.all([         getHomeBookingSettings(),         appointmentService.countHomeUserActiveAppointments(homeUser.id),         getBarbers({ includeInactive: false }),         getServiceCatalog(false),       ]);       const barber = barbers.find((item) => normalizeText(item.id) === barberId);       if (!barber) {         return res.status(404).json({ error: "Р‘Р°СЂР±РµСЂ РЅРµ РЅР°Р№РґРµРЅ." });       }       const services = appointmentService.resolveBookableServicesForBarber(         servicesCatalog,         barber.id,       );       const canBook = activeAppointments < settings.bookingLimit;       const message = canBook         ? ""         : `РЈ РІР°СЃ СѓР¶Рµ РµСЃС‚СЊ ${settings.bookingLimit} Р°РєС‚РёРІРЅС‹С… Р·Р°РїРёСЃРµР№.`;        return res.json({         canBook,         message,         bookingLimit: settings.bookingLimit,         activeAppointments,         barber: {           id: barber.id,           name: normalizeText(barber.name),           phrase: normalizeText(barber.cardPhrase) || normalizeText(barber.description),         },         services,       });     } catch (error) {       console.error("Home booking services error:", error);       return res.status(500).json({ error: "РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РіСЂСѓР·РёС‚СЊ СѓСЃР»СѓРіРё РґР»СЏ Р·Р°РїРёСЃРё." });     }   });    app.get("/api/home/booking/dates", authenticateHomeToken, async (req, res) => {     try {       const homeUser = await resolveHomeBookingUser(req);       if (!homeUser) return res.sendStatus(401);       const barberId = normalizeText(req.query?.barberId);       const serviceIds = appointmentService.parseServiceIdsInput(req.query?.serviceIds);       if (!barberId || !serviceIds.length) {         return res.status(400).json({ error: "Р’С‹Р±РµСЂРёС‚Рµ Р±Р°СЂР±РµСЂР° Рё СѓСЃР»СѓРіСѓ." });       }        const [settings, activeAppointments, barbers, servicesCatalog] = await Promise.all([         getHomeBookingSettings(),         appointmentService.countHomeUserActiveAppointments(homeUser.id),         getBarbers({ includeInactive: false }),         getServiceCatalog(false),       ]);       if (activeAppointments >= settings.bookingLimit) {         return res.status(409).json({           error: `РЈ РІР°СЃ СѓР¶Рµ РµСЃС‚СЊ ${settings.bookingLimit} Р°РєС‚РёРІРЅС‹С… Р·Р°РїРёСЃРµР№.`,         });       }       const barber = barbers.find((item) => normalizeText(item.id) === barberId);       if (!barber) {         return res.status(404).json({ error: "Р‘Р°СЂР±РµСЂ РЅРµ РЅР°Р№РґРµРЅ." });       }        const bookableServices = appointmentService.resolveBookableServicesForBarber(         servicesCatalog,         barber.id,       );       const selectedServices = serviceIds         .map((id) => bookableServices.find((service) => service.id === id))         .filter(Boolean);       if (!selectedServices.length) {         return res.status(400).json({ error: "РЈ РІС‹Р±СЂР°РЅРЅРѕРіРѕ Р±Р°СЂР±РµСЂР° РЅРµС‚ С‚Р°РєРѕР№ СѓСЃР»СѓРіРё." });       }        const totalDuration = Math.max(         selectedServices.reduce(           (sum, service) => sum + Math.max(0, Number(service.duration) || 0),           0,         ),         15,       );       const minAllowedDate = new Date(Date.now() + settings.minLeadHours * 60 * 60 * 1000);       const dateWindow = buildDateWindow(settings.maxDaysAhead);       const dates = [];        for (const entry of dateWindow) {         const workingHours = await appointmentService.getWorkingHoursForBarberDate(           barber.name,           entry.key,         );         if (!workingHours) continue;         const busyIntervals = await appointmentService.getBusyIntervalsForBarberDate(           barber.name,           entry.key,         );         const slots = appointmentService.buildTimeSlotsForDate({           dateKey: entry.key,           workingHours,           busyIntervals,           totalDuration,           minAllowedDate,         });         if (slots.length) dates.push(entry.key);       }        return res.json({         dateCount: dates.length,         dates,         totalDuration,         services: selectedServices,       });     } catch (error) {       console.error("Home booking dates error:", error);       return res.status(500).json({ error: "РќРµ СѓРґР°Р»РѕСЃСЊ РїРѕР»СѓС‡РёС‚СЊ РґРѕСЃС‚СѓРїРЅС‹Рµ РґР°С‚С‹." });     }   });    app.get("/api/home/booking/times", authenticateHomeToken, async (req, res) => {     try {       const homeUser = await resolveHomeBookingUser(req);       if (!homeUser) return res.sendStatus(401);       const barberId = normalizeText(req.query?.barberId);       const serviceIds = appointmentService.parseServiceIdsInput(req.query?.serviceIds);       const dateKey = normalizeText(req.query?.date);       if (!barberId || !serviceIds.length || !dateKey) {         return res.status(400).json({ error: "Р’С‹Р±РµСЂРёС‚Рµ СѓСЃР»СѓРіСѓ Рё РґР°С‚Сѓ." });       }       if (!appointmentService.isIsoDateKey(dateKey)) {         return res.status(400).json({ error: "РќРµРєРѕСЂСЂРµРєС‚РЅР°СЏ РґР°С‚Р°." });       }        const [settings, activeAppointments, barbers, servicesCatalog] = await Promise.all([         getHomeBookingSettings(),         appointmentService.countHomeUserActiveAppointments(homeUser.id),         getBarbers({ includeInactive: false }),         getServiceCatalog(false),       ]);       if (activeAppointments >= settings.bookingLimit) {         return res.status(409).json({           error: `РЈ РІР°СЃ СѓР¶Рµ РµСЃС‚СЊ ${settings.bookingLimit} Р°РєС‚РёРІРЅС‹С… Р·Р°РїРёСЃРµР№.`,         });       }       const barber = barbers.find((item) => normalizeText(item.id) === barberId);       if (!barber) {         return res.status(404).json({ error: "Р‘Р°СЂР±РµСЂ РЅРµ РЅР°Р№РґРµРЅ." });       }       const bookableServices = appointmentService.resolveBookableServicesForBarber(         servicesCatalog,         barber.id,       );       const selectedServices = serviceIds         .map((id) => bookableServices.find((service) => service.id === id))         .filter(Boolean);       if (!selectedServices.length) {         return res.status(400).json({ error: "РЈ РІС‹Р±СЂР°РЅРЅРѕРіРѕ Р±Р°СЂР±РµСЂР° РЅРµС‚ С‚Р°РєРѕР№ СѓСЃР»СѓРіРё." });       }        const totalDuration = Math.max(         selectedServices.reduce(           (sum, service) => sum + Math.max(0, Number(service.duration) || 0),           0,         ),         15,       );       const workingHours = await appointmentService.getWorkingHoursForBarberDate(         barber.name,         dateKey,       );       if (!workingHours) {         return res.json({ date: dateKey, totalDuration, times: [] });       }       const busyIntervals = await appointmentService.getBusyIntervalsForBarberDate(         barber.name,         dateKey,       );       const minAllowedDate = new Date(Date.now() + settings.minLeadHours * 60 * 60 * 1000);       const times = appointmentService.buildTimeSlotsForDate({         dateKey,         workingHours,         busyIntervals,         totalDuration,         minAllowedDate,       });        return res.json({         date: dateKey,         totalDuration,         times,       });     } catch (error) {       console.error("Home booking times error:", error);       return res.status(500).json({ error: "РќРµ СѓРґР°Р»РѕСЃСЊ РїРѕР»СѓС‡РёС‚СЊ РґРѕСЃС‚СѓРїРЅРѕРµ РІСЂРµРјСЏ." });     }   });    app.post("/api/home/booking/appointments", authenticateHomeToken, async (req, res) => {     try {       const homeUser = await resolveHomeBookingUser(req);       if (!homeUser) return res.sendStatus(401);        const barberId = normalizeText(req.body?.barberId);       const serviceIds = appointmentService.parseServiceIdsInput(req.body?.serviceIds);       const dateKey = normalizeText(req.body?.date);       const startTime = normalizeText(req.body?.startTime);       if (!barberId || !serviceIds.length || !dateKey || !startTime) {         return res           .status(400)           .json({ error: "Р—Р°РїРѕР»РЅРёС‚Рµ Р±Р°СЂР±РµСЂР°, СѓСЃР»СѓРіСѓ, РґР°С‚Сѓ Рё РІСЂРµРјСЏ." });       }       if (!appointmentService.isIsoDateKey(dateKey)) {         return res.status(400).json({ error: "РќРµРєРѕСЂСЂРµРєС‚РЅР°СЏ РґР°С‚Р°." });       }       const startMinute = appointmentService.parseTimeLabelToMinutes(startTime);       if (startMinute == null) {         return res.status(400).json({ error: "РќРµРєРѕСЂСЂРµРєС‚РЅРѕРµ РІСЂРµРјСЏ." });       }        const [settings, barbers, servicesCatalog] = await Promise.all([         getHomeBookingSettings(),         getBarbers({ includeInactive: false }),         getServiceCatalog(false),       ]);       const barber = barbers.find((item) => normalizeText(item.id) === barberId);       if (!barber) {         return res.status(404).json({ error: "Р‘Р°СЂР±РµСЂ РЅРµ РЅР°Р№РґРµРЅ." });       }       const bookableServices = appointmentService.resolveBookableServicesForBarber(         servicesCatalog,         barber.id,       );       const selectedServices = serviceIds         .map((id) => bookableServices.find((service) => service.id === id))         .filter(Boolean);       if (!selectedServices.length) {         return res.status(400).json({ error: "РЈ РІС‹Р±СЂР°РЅРЅРѕРіРѕ Р±Р°СЂР±РµСЂР° РЅРµС‚ С‚Р°РєРѕР№ СѓСЃР»СѓРіРё." });       }       const totalDuration = Math.max(         selectedServices.reduce(           (sum, service) => sum + Math.max(0, Number(service.duration) || 0),           0,         ),         15,       );       let createdAppointment = null;       try {         createdAppointment = await appointmentService.createHomeAppointment({           homeUser,           barber,           dateKey,           startMinute,           totalDuration,           selectedServices,           settings,           activeStatus: STATUS_ACTIVE,         });       } catch (error) {         if (error?.message === "LIMIT_REACHED") {           return res.status(409).json({             error: `РЈ РІР°СЃ СѓР¶Рµ РµСЃС‚СЊ ${settings.bookingLimit} Р°РєС‚РёРІРЅС‹С… Р·Р°РїРёСЃРµР№.`,           });         }         if (error?.message === "NO_SCHEDULE") {           return res             .status(409)             .json({ error: "РќР° РІС‹Р±СЂР°РЅРЅСѓСЋ РґР°С‚Сѓ Сѓ Р±Р°СЂР±РµСЂР° РЅРµС‚ СЂР°СЃРїРёСЃР°РЅРёСЏ." });         }         if (error?.message === "OUTSIDE_WORKING_HOURS") {           return res             .status(409)             .json({ error: "Р’СЂРµРјСЏ РІС‹С…РѕРґРёС‚ Р·Р° СЂР°Р±РѕС‡РёР№ РґРёР°РїР°Р·РѕРЅ Р±Р°СЂР±РµСЂР°." });         }         if (error?.message === "LEAD_TIME") {           return res             .status(409)             .json({ error: "Р­С‚Рѕ РІСЂРµРјСЏ РЅРµРґРѕСЃС‚СѓРїРЅРѕ РёР·-Р·Р° РѕРіСЂР°РЅРёС‡РµРЅРёСЏ minLeadHours." });         }         if (error?.message === "SLOT_TAKEN") {           return res             .status(409)             .json({ error: "РЎР»РѕС‚ СѓР¶Рµ Р·Р°РЅСЏС‚. Р’С‹Р±РµСЂРёС‚Рµ РґСЂСѓРіРѕРµ РІСЂРµРјСЏ." });         }         throw error;       }       await notifyBarberAboutNewAppointment({         appointment: createdAppointment,         barber,         homeUser,       });       requestRealtimePush(true);        return res.status(201).json({         appointment: {           id: createdAppointment.id,           barberName: createdAppointment.Barber,           date: createdAppointment.Date,           time: createdAppointment.Time,           services: createdAppointment.Services,         },       });     } catch (error) {       console.error("Home booking create error:", error);       return res.status(500).json({ error: "РќРµ СѓРґР°Р»РѕСЃСЊ СЃРѕР·РґР°С‚СЊ Р·Р°РїРёСЃСЊ." });     }   });    app.post("/api/home/booking/appointments/:id/cancel", authenticateHomeToken, async (req, res) => {     try {       const homeUser = await resolveHomeBookingUser(req);       if (!homeUser) return res.sendStatus(401);       const appointmentId = normalizeText(req.params.id);       if (!appointmentId) {         return res.status(400).json({ error: "РќРµ СѓРєР°Р·Р°РЅР° Р·Р°РїРёСЃСЊ." });       }       const existing = await prisma.appointments.findUnique({ where: { id: appointmentId } });       if (!existing) {         return res.status(404).json({ error: "Р—Р°РїРёСЃСЊ РЅРµ РЅР°Р№РґРµРЅР°." });       }       if (normalizeText(existing.UserID) !== normalizeText(homeUser.id)) {         return res.status(403).json({ error: "РќРµР»СЊР·СЏ РѕС‚РјРµРЅРёС‚СЊ С‡СѓР¶СѓСЋ Р·Р°РїРёСЃСЊ." });       }       if (normalizeText(existing.Status) !== normalizeText(STATUS_ACTIVE)) {         return res.status(409).json({ error: "Р­С‚Сѓ Р·Р°РїРёСЃСЊ СѓР¶Рµ РЅРµР»СЊР·СЏ РѕС‚РјРµРЅРёС‚СЊ." });       }       const appointmentStart = parseDateTime(existing.Date, existing.Time);       if (appointmentStart && appointmentStart.getTime() - Date.now() < 2 * 60 * 60 * 1000) {         return res.status(409).json({           code: "TOO_LATE_TO_CANCEL",           error: "Р—Р°РїРёСЃСЊ РЅРµР»СЊР·СЏ РѕС‚РјРµРЅРёС‚СЊ РјРµРЅРµРµ С‡РµРј Р·Р° 2 С‡Р°СЃР° РґРѕ РЅР°С‡Р°Р»Р°.",         });       }       const updated = await prisma.appointments.update({         where: { id: appointmentId },         data: { Status: STATUS_CANCELLED },       });       requestRealtimePush(true);       return res.json({ appointment: updated });     } catch (error) {       console.error("Home booking cancel error:", error);       return res.status(500).json({ error: "РќРµ СѓРґР°Р»РѕСЃСЊ РѕС‚РјРµРЅРёС‚СЊ Р·Р°РїРёСЃСЊ." });     }   }); };  module.exports = {   registerHomeRoutes, };